# 核心框架
Flask==3.1.1
flask-cors==4.0.0
gunicorn==23.0.0
Werkzeug==3.1.3

# 表格与导出
pandas==2.3.1
openpyxl==3.1.2   # pandas.to_excel 需要

# Word 文档解析
python-docx==1.2.0

# PDF 解析
PyMuPDF==1.26.3

# OCR 文字识别
easyocr==1.7.2
torch==2.1.0   # 选轻量版，如 CPU-only, 不要带 CUDA

# 图像处理
opencv-python-headless==4.9.0.80
