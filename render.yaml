services:
  - type: web
    name: omr-render-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: ".venv/bin/gunicorn --bind 0.0.0.0:$PORT --timeout 120 main:app"
